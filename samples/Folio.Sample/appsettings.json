{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//": "Plaintext credentials are fine for this local sample, but never for a real app — hash and salt passwords (or use ASP.NET Core Identity / an external provider) instead.",
  "Users": [
    { "Username": "admin", "Password": "admin" },
    { "Username": "viewer", "Password": "viewer123" }
  ]
}
